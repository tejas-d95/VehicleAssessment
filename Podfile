# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VehicleAssessment' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for VehicleAssessment
	pod 'Kingfisher', '~> 7.0'

  target 'VehicleAssessmentTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VehicleAssessmentUITests' do
    # Pods for testing
  end

end
